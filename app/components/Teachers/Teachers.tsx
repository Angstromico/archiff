import { TeachersCarousel, TextTeachers } from './components'

const Teachers = () => {
  return (
    <section className='mt-24 md:mt-52'>
      <TextTeachers />
      <TeachersCarousel />
    </section>
  )
}

export default Teachers
